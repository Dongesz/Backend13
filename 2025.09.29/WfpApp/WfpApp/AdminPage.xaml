<Page x:Class="WfpApp.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WfpApp"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid Margin="16">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cím -->
        <TextBlock Grid.ColumnSpan="2" 
                   Text="Admin Panel"
                   FontSize="18" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- ListBox a felhasználókhoz -->
        <ListBox x:Name="UsersListBox" 
                 Grid.Row="1" Grid.Column="0" 
                 Margin="0,10,10,10"
                 SelectionMode="Single"/>

        <!-- Részletek és műveletek -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="10,10,0,10">
            <Label x:Name="SelectedUserLabel" Content="Selected User: -" FontSize="14" Margin="0,0,0,10"/>

            <Button x:Name="DeleteButton" Content="Törlés" Width="100" Margin="0,5,0,0"/>
            <Button x:Name="EditButton" Content="Módosítás" Width="100" Margin="0,5,0,0"/>
            <Button x:Name="RefreshButton" Content="Frissítés" Width="100" Margin="0,5,0,0"/>
        </StackPanel>

    </Grid>
</Page>
